const assert = require('assert');
const extend = require('extend');

describe('postmark-templates', () => {

  describe('test', () => {

    it('it should return highlighted JSON', () => {
      assert.equal(true, true);
    });
  });

});
